# Export with a pack directive

A plain export never loads packs, so this directive from the (not
installed here) "ana" pack falls back to the unknown-component box.

::ana_timeline
No timeline data in a plain export.
::
