:::chart{width=fit}
A chart shrunk to its own content.
:::

:::chart{width=narrow}
A narrow chart.
:::

:::chart{width=normal}
A normal-width chart.
:::

:::chart{width=wide}
A wide chart.
:::

:::chart{width=full}
A full-width chart.
:::

::img-pair{align=left}

::img-pair{align=center}

::img-pair{align=right}

:::row{cols=2}
Two cells.
:::

:::row{cols=99}
An out-of-range cols value — layout defaults it to auto-fit at render time,
but the parser passes it through unchanged.
:::
