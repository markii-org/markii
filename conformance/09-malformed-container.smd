:::callout{type=danger title="Unclosed"}
This container directive is never closed with a matching `:::` — parsing
and rendering must not throw, even though the document is malformed.

More text after it, still inside the still-open container.
