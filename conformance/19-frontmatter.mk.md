---
title: Release notes
uses: [ana, gh]
---

# Release notes

Frontmatter parses as a `yaml` node carrying the raw block text — the parser
never interprets YAML — and the renderer drops it from the output.

:::callout{type=note}
The body after the closing fence is ordinary markdown.
:::
