{
  "name": "markii-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/platforms/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present && npm run typecheck:packs",
    "typecheck:packs": "tsc -p packs/tsconfig.json",
    "build:dist": "npm run build:dist -w @markii/core && npm run build:dist -w @markii/stdlib && npm run build:dist -w @markii/bundle && npm run build:dist -w @markii/pack && npm run build:dist -w @markii/runtime && npm run build:dist -w @markii/lua && npm run build:dist -w @markii/react && npm run build:dist -w @markii/html",
    "lint": "eslint . && prettier --check .",
    "dev": "npm run dev --workspace apps/playground"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@vitejs/plugin-react": "^6.0.5",
    "eslint": "^10.8.1",
    "jsdom": "^30.0.1",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.67.0",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
