{
  "name": "@markii/host",
  "version": "0.1.0",
  "description": "Shared, host-neutral script-running layer for Markii hosts (apps/vscode, and any future host): tier gating, the grant model, DNS/address pinning, and the terminatable-isolate watchdog around the worker_thread Run path. PRIVATE — never published, exists only to be shared between apps in this repo.",
  "license": "MIT",
  "author": "sadigaxund",
  "private": true,
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./browser": {
      "types": "./src/browser.ts",
      "import": "./src/browser.ts",
      "default": "./src/browser.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint ."
  },
  "dependencies": {
    "@markii/bundle": "*",
    "@markii/core": "*",
    "@markii/lua": "*",
    "@markii/pack": "*",
    "@markii/react": "*",
    "@markii/runtime": "*",
    "@markii/stdlib": "*",
    "esbuild-wasm": "^0.28.2"
  }
}
