/** A helper's own further helper — imported extensionless from `./helper.ts` (`import { deepValue } from './deep'`). Its return value is a marker string a test asserts appears literally in the compiled output, proving this file's code actually made it into the bundle. */
export function deepValue(): string {
  return 'deep-marker-9f3c';
}
